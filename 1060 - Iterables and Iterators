import itertools
N = int(input())
ltr = list(map(str, input().split()))
K = int(input())
count = 0
lst = list(itertools.combinations(ltr, K))
for item in (lst):
    if 'a' in item:
        count += 1

print('%.3f' %(count/len(lst)))
